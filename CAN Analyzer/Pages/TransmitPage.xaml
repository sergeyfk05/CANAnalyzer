<UserControl x:Class="CANAnalyzer.Pages.TransmitPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:resources="clr-namespace:CANAnalyzer.Resources.DynamicResources"
             xmlns:local="clr-namespace:CANAnalyzer.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TransmitPageTopButtonStyle">
            <Setter Property="Control.Background">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="{resources:Theme TransmitPageTopButtonBackgroundStartBrush}" Offset="0"/>
                        <GradientStop Color="{resources:Theme TransmitPageTopButtonBackgroundStopBrush}" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Control.BorderThickness" Value="{resources:Theme TransmitPageTopButtonBorderThickness}"/>
            <Setter Property="Control.BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="{resources:Theme TransmitPageTopButtonBorderStartBrush}" Offset="0"/>
                        <GradientStop Color="{resources:Theme TransmitPageTopButtonBorderStopBrush}" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Control.Width" Value="30"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="{resources:Theme TransmitPageTopButtonCornerRaduis}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Button.IsMouseOver" Value="True">
                    <Setter Property="Control.Background">
                        <Setter.Value>
                            <LinearGradientBrush>
                                <GradientStop Color="{resources:Theme TransmitPageTopButtonMouseEnterBackgroundStartBrush}" Offset="0"/>
                                <GradientStop Color="{resources:Theme TransmitPageTopButtonMouseEnterBackgroundStopBrush}" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TransmitPageTopButtonImageStyle">
            <Setter Property="Control.Width" Value="15"/>
            <Setter Property="Control.Height" Value="15"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Button x:Name="openButton" DockPanel.Dock="Left" Style="{DynamicResource TransmitPageTopButtonStyle}">
                <Image Style="{DynamicResource TransmitPageTopButtonImageStyle}" Source="{resources:Theme TransmitPageOpenButtonIcon}"/>

                <Button.ToolTip>
                    <ToolTip Content="{resources:Language TransmitPageOpenButtonToolTip}"/>
                </Button.ToolTip>
            </Button>

            <Button x:Name="saveButton" DockPanel.Dock="Left" Style="{DynamicResource TransmitPageTopButtonStyle}">
                <Image Style="{DynamicResource TransmitPageTopButtonImageStyle}" Source="{resources:Theme TransmitPageSaveButtonIcon}"/>

                <Button.ToolTip>
                    <ToolTip Content="{resources:Language TransmitPageSaveButtonToolTip}"/>
                </Button.ToolTip>
            </Button>

            <Button x:Name="clearButton" DockPanel.Dock="Left" Style="{DynamicResource TransmitPageTopButtonStyle}">
                <Image Style="{DynamicResource TransmitPageTopButtonImageStyle}" Source="{resources:Theme TransmitPageClearButtonIcon}"/>

                <Button.ToolTip>
                    <ToolTip Content="{resources:Language TransmitPageClearButtonToolTip}"/>
                </Button.ToolTip>
            </Button>
        </DockPanel>
        
        
        <DataGrid Grid.Column="0" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
